import { defineMessages } from 'react-intl';

export const headings = defineMessages({
  rawName: { id: 'rawPage.rawTable.heading.name', defaultMessage: 'Name' },
  price: { id: 'rawPage.rawTable.heading.price', defaultMessage: 'Price' },
});
